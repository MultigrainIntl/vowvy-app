name: JoieOS Compliance

# This repository does not define its own rules. It inherits them.
# The rules live in MultigrainIntl/joieos-governance and are edited there, once, for everyone.
# Pinned to tag v1 on purpose: a rule change must be a deliberate act, not automatic.
on: [push, pull_request, workflow_dispatch]

jobs:
  compliance:
    uses: MultigrainIntl/joieos-governance/.github/workflows/compliance.yml@v1
